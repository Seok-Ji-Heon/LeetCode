class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        
        for i in range(len(nums)):
            temp = nums[0]
            nums.remove(temp)
            if temp not in nums:
                return temp
            nums.append(temp)
