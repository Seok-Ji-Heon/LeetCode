class Solution:
    def checkOnesSegment(self, s: str) -> bool:
        
        if '0' in s:
            idx = s.index('0')
        else:
            idx = len(s)
        
        return False if '1' in s[idx:] else True
