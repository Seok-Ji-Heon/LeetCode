class Solution:
    def largestAltitude(self, gain: List[int]) -> int:
        
        mx = 0
        temp = 0
        for num in gain :
            temp +=num
            
            if temp>mx:
                mx = temp
        
        return mx
