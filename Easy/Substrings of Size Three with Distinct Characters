class Solution:
    def countGoodSubstrings(self, s: str) -> int:
        cnt = 0
        for i in range(len(s)-2):
            substring = s[i:i+3]
            if len(substring)==len(set(substring)):
                cnt+=1
        
        return cnt
